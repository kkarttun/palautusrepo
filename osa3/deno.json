{
    "deploy": {
        "install": "npm install --prefix phonebookFrontend && npm install --prefix phonebookBackend",
        "build": "npm run build --prefix phonebookFrontend"},
    "runtime": {"entrypoint": "./phonebookBackend/index.js"}
}